{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if jq -r '.tool_input.command // \"\"' | grep -qE '^git commit'; then bundle exec rubocop lib/ test/; fi",
            "timeout": 60,
            "statusMessage": "Running rubocop lint check..."
          }
        ]
      }
    ]
  }
}
